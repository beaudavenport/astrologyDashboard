require 'test_helper'

class OrderTest < ActiveSupport::TestCase
  test "order should only save if service id is set" do
    
  end
  
  test "order should only save if status is 'Completed' " do
  end
  
end
